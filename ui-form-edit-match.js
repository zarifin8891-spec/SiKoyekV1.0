/* Compatibility shim only. Edit Proyek is rendered exclusively by ui-form-edit-final-v5.js. */
(function(){
  /* Intentionally no DOM mutation, no CSS overrides, no observers, no renderer. */
  window.__sikoyekEditMatchDisabled=true;
})();
