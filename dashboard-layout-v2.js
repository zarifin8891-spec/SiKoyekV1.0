/* Dashboard Layout V2 — stable presentation helper + compact lower-panel layout. */
(function(){
'use strict';
const qs=(s,r=document)=>r.querySelector(s),qsa=(s,r=document)=>[...r.querySelectorAll(s)];
function isDashboard(){const h=qs('.top h1');return !!h&&h.textContent.trim().toLowerCase()==='dashboard'}
function dashboard(){return qs('.dashboard-view')||qs('.content')}
function formatDateTime(date){return new Intl.DateTimeFormat('id-ID',{weekday:'short',day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',hour12:false}).format(date).replace(',',' •')}
function movePeriodIntoHeader(top){const period=qs('.periodbar');if(period&&top&&period.parentElement!==top)top.appendChild(period)}
function stabilizePeriodPreset(root){const preset=qs('#periodPreset',root);if(!preset)return;const key='sikoyek.dashboard.periodPreset';if(!preset.dataset.stableBound){preset.dataset.stableBound='1';preset.addEventListener('change',()=>preset.value?localStorage.setItem(key,preset.value):localStorage.removeItem(key))}const saved=localStorage.getItem(key);if(saved&&[...preset.options].some(o=>o.value===saved)&&preset.value!==saved)preset.value=saved;else if(preset.value)localStorage.setItem(key,preset.value)}
function normalizePercentages(root){qsa('.kpi .value,.table td,.big',root).forEach(el=>{const m=(el.textContent||'').trim().match(/^(-?\d+(?:\.\d+)?)%$/);if(!m)return;const v=Number(m[1]);if(Math.abs(v)>0&&Math.abs(v)<=1)el.textContent=(v*100).toFixed(2)+'%'})}
function normalizeMoneyKpis(root){qsa('.dashboard-kpi-strip .kpi .value',root).slice(0,4).forEach(el=>el.textContent=(el.textContent||'').replace(/^\s*Rp\.?\s*/i,'').trim())}
function compactKpis(){const strip=qs('.dashboard-view .cards'),health=qs('.dashboard-view .health');if(!strip||!health)return;strip.classList.add('dashboard-kpi-strip');qsa(':scope > *',health).forEach(box=>strip.appendChild(box));const section=health.closest('.section');if(section?.parentNode)section.remove()}
function decorateKpis(){const strip=qs('.dashboard-view .dashboard-kpi-strip');if(!strip)return;const icons={'TOTAL PROYEK':'▣','NILAI KONTRAK':'▤','TOTAL RAP':'▤','TOTAL REALISASI':'▥','AVG PROGRESS':'↗'};qsa('.kpi',strip).forEach(k=>{if(qs('.kpi-icon',k))return;const label=(qs('.label',k)?.textContent||'').trim();if(!icons[label])return;const i=document.createElement('span');i.className='kpi-icon';i.textContent=icons[label];k.insertBefore(i,k.firstChild);k.classList.add('reference-kpi')})}
function ensureMasterDataNav(){if(!isDashboard())return;const nav=qs('.sidebar .nav');if(!nav||qs('[data-master-data-nav]',nav))return;const b=document.createElement('button');b.type='button';b.dataset.masterDataNav='1';b.textContent='Master Data';b.onclick=()=>window.openMasterData?.();nav.appendChild(b)}
function compactHeaderTypography(top){const h1=qs('h1',top),p=qs('p',top);if(h1)h1.style.cssText+=';font-size:28px!important;line-height:1.08!important;font-weight:700!important';if(p)p.style.cssText+=';font-size:12px!important;line-height:1.2!important;font-weight:400!important';qsa('.periodrow .field label',top).forEach(x=>x.style.cssText+=';font-size:10px!important;font-weight:500!important');qsa('.periodrow input,.periodrow select,.periodrow .btn',top).forEach(x=>x.style.cssText+=';font-size:14px!important;font-weight:400!important');qsa('.periodrow .btn,.actions .btn',top).forEach(x=>x.style.cssText+=';font-weight:500!important');qsa('.periodnote',top).forEach(x=>x.style.cssText+=';font-size:10px!important;font-weight:400!important')}
function findSection(root,names){return qsa('.section',root).find(sec=>names.includes((qs('.sectiontitle h2',sec)?.textContent||'').replace(/\s+/g,' ').trim()))}
function twoLine(a,b){return `<span class="two-line">${a}<br>${b}</span>`}
function prepareHealth(section){if(!section)return;section.classList.add('dashboard-static-health');const title=qs('.sectiontitle h2',section);if(title)title.textContent='Kondisi Proyek';const note=qs('.sectiontitle .note',section);if(note)note.textContent='Progress vs Rasio Biaya & RAP Terpakai';const table=qs('.table',section);if(!table)return;table.classList.add('health-static-table');const head=qs('thead tr',table);if(head){const labels=['Proyek','Progress',twoLine('Rasio','Biaya'),twoLine('RAP','Terpakai'),'Status'];while(head.children.length>5)head.lastElementChild.remove();labels.forEach((v,i)=>{if(head.children[i])head.children[i].innerHTML=v})}qsa('tbody tr',table).forEach(tr=>{while(tr.children.length>5)tr.lastElementChild.remove()})}
function prepareDecision(section){if(!section)return;section.classList.add('dashboard-static-decision');const title=qs('.sectiontitle h2',section);if(title)title.textContent='Prioritas & Tindakan';const note=qs('.sectiontitle .note',section);if(note)note.textContent='Rekomendasi berbasis Kondisi Proyek';const table=qs('.table',section);if(!table)return;table.classList.add('decision-static-table');const head=qs('thead tr',table);if(head){const labels=['Proyek','Prioritas','Status',twoLine('Masalah','Utama'),'Tindakan'];while(head.children.length>5)head.lastElementChild.remove();labels.forEach((v,i)=>{if(head.children[i])head.children[i].innerHTML=v;else{const th=document.createElement('th');th.innerHTML=v;head.appendChild(th)}})}qsa('tbody tr',table).forEach(tr=>{if(tr.children.length>=5)return;const td=document.createElement('td');td.textContent='Pertahankan kontrol dan lanjutkan pemantauan rutin.';tr.appendChild(td)})}
function organizeStaticLowerPanels(){const d=qs('.dashboard-view');if(!d)return;const health=findSection(d,['Project Health','Kesehatan Proyek','Kondisi Proyek']),decision=findSection(d,['Prioritas & Tindakan']);if(!health&&!decision)return;let grid=qs('.dashboard-lower-v3-grid',d);if(!grid){grid=document.createElement('div');grid.className='dashboard-lower-v3-grid';const anchor=health||decision;if(anchor?.parentNode)anchor.parentNode.insertBefore(grid,anchor);else d.appendChild(grid)}if(health&&health.parentNode!==grid)grid.appendChild(health);if(decision&&decision.parentNode!==grid)grid.appendChild(decision);prepareHealth(health);prepareDecision(decision);qsa('#health-engine-v1-card,#decision-engine-v1-card',d).forEach(x=>x.style.setProperty('display','none','important'))}
function requestPanelSync(){if(!isDashboard())return;const request=()=>window.dispatchEvent(new CustomEvent('sikoyek:dashboard-panel-request'));request();setTimeout(request,500);setTimeout(request,1200)}
function apply(){const content=dashboard(),top=qs('.top');if(!content||!top)return;if(!isDashboard()){content.classList.remove('dashboard-view');top.classList.remove('dashboard-top');return}content.classList.add('dashboard-view');top.classList.add('dashboard-top');movePeriodIntoHeader(top);stabilizePeriodPreset(content);const actions=qs('.actions',top);if(!actions)return;let info=qs('.dashboard-userinfo',actions);if(!info){info=document.createElement('div');info.className='dashboard-userinfo';info.innerHTML='<span class="user-copy"><div class="user-main">Pengguna</div><div class="user-time"></div></span>';actions.insertBefore(info,actions.firstChild)}const userMain=qs('.user-main',info),userTime=qs('.user-time',info),client=window.__siKoyekSupabase;if(client?.auth?.getSession)client.auth.getSession().then(({data})=>{const identity=data?.session?.user?.user_metadata?.full_name||data?.session?.user?.email||'Pengguna';if(userMain)userMain.textContent=identity}).catch(()=>{});if(userTime)userTime.textContent=formatDateTime(new Date());compactHeaderTypography(top);compactKpis();decorateKpis();normalizePercentages(content);normalizeMoneyKpis(content);organizeStaticLowerPanels();ensureMasterDataNav();requestPanelSync()}
let scheduled=false;function schedule(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;try{apply()}catch(e){console.warn('Dashboard layout helper:',e)}})}
new MutationObserver(schedule).observe(document.body,{childList:true,subtree:true});setInterval(()=>{if(isDashboard()){const t=qs('.dashboard-userinfo .user-time');if(t)t.textContent=formatDateTime(new Date())}},30000);schedule();
})();
