name: Retired Dashboard Render Hotfix

on:
  workflow_dispatch:

# Retained for traceability only. Publishing is exclusively owned by pages.yml.
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired: Dashboard rendering is deployed only by pages.yml."
