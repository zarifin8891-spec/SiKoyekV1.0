name: Retired Dashboard Final Hotfix

on:
  workflow_dispatch:

# Retained for traceability only. It must not patch or publish a Pages artifact.
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired: canonical Dashboard changes belong in source files."
