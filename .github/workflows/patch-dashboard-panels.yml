name: Retired Dashboard Panel Patcher

on:
  workflow_dispatch:

# Retained for traceability only. This workflow performs no mutation or publish.
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired: no automated Dashboard patching or commits."
