name: Patch Dashboard Lower Panels

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch-dashboard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Patch index.html dashboard panels
        shell: python
        run: |
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          original = s

          # Keep the already-approved header/KPI implementation untouched.
          s = s.replace('>PERLU PENGAWASAN</div></div><div class="dashboard-status red">', '>AWASI</div></div><div class="dashboard-status red">')
          s = s.replace('<h2>Project Health</h2><span class="note">Progress vs Cost Ratio &amp; RAP Consumption</span>', '<h2>Kesehatan Proyek</h2><span class="note">Progress vs Rasio Biaya &amp; RAP Terpakai</span>')
          s = s.replace('<th>Proyek</th><th>Progress</th><th>Cost Ratio</th><th>RAP<br>Consumption</th><th>Health</th><th>Gap Cost</th>', '<th>Proyek</th><th>Progress</th><th>Rasio<br>Biaya</th><th>RAP<br>Terpakai</th><th>Status</th>')
          s = s.replace('<td><span class="pill ${healthClass(x.health_status)}">${esc(x.health_status)}</span></td><td>${Number(x.cost_ratio||0)-Number(x.rap_consumption||0>=0)?\'\':\'\'}${pct(Number(x.cost_ratio||0)-Number(x.rap_consumption||0))}</td>', '<td><span class="pill ${healthClass(x.health_status)}">${esc(x.health_status)}</span></td>')
          s = s.replace('<span class="note">Rekomendasi berbasis Project Health</span>', '<span class="note">Rekomendasi berbasis Kesehatan Proyek</span>')
          s = s.replace('<th>Proyek</th><th>Prioritas</th><th>Status</th><th>Masalah Utama</th><th>Tindakan</th>', '<th>Proyek</th><th>Prioritas</th><th>Status</th><th>Masalah<br>Utama</th><th>Tindakan</th>')

          css = '''\n/* SiKoyek V1.0 — approved lower dashboard panel layout */\n.dashboard-panels{grid-template-columns:minmax(0,44fr) minmax(0,56fr)!important;gap:14px!important}\n.dashboard-panel{min-width:0!important}\n.dashboard-panel .table{width:100%!important;table-layout:fixed!important}\n.dashboard-panel .table th,.dashboard-panel .table td{padding:10px 10px!important;font-size:12px!important;line-height:1.25!important;vertical-align:middle!important}\n.dashboard-panel .table th{font-size:9px!important;white-space:normal!important}\n.dashboard-panel:first-child .table th:nth-child(1),.dashboard-panel:first-child .table td:nth-child(1){width:41%!important}\n.dashboard-panel:first-child .table th:nth-child(2),.dashboard-panel:first-child .table td:nth-child(2){width:12%!important}\n.dashboard-panel:first-child .table th:nth-child(3),.dashboard-panel:first-child .table td:nth-child(3){width:14%!important}\n.dashboard-panel:first-child .table th:nth-child(4),.dashboard-panel:first-child .table td:nth-child(4){width:18%!important}\n.dashboard-panel:first-child .table th:nth-child(5),.dashboard-panel:first-child .table td:nth-child(5){width:15%!important}\n.dashboard-panel:first-child .table td:first-child{white-space:normal!important}\n.dashboard-panel:last-child .table th:nth-child(1),.dashboard-panel:last-child .table td:nth-child(1){width:32%!important}\n.dashboard-panel:last-child .table th:nth-child(2),.dashboard-panel:last-child .table td:nth-child(2){width:12%!important}\n.dashboard-panel:last-child .table th:nth-child(3),.dashboard-panel:last-child .table td:nth-child(3){width:11%!important}\n.dashboard-panel:last-child .table th:nth-child(4),.dashboard-panel:last-child .table td:nth-child(4){width:22%!important;white-space:normal!important}\n.dashboard-panel:last-child .table th:nth-child(5),.dashboard-panel:last-child .table td:nth-child(5){width:23%!important;white-space:normal!important}\n.dashboard-panel:last-child .table td:first-child{white-space:normal!important}\n@media(max-width:1100px){.dashboard-panels{grid-template-columns:1fr!important}}\n'''
          if '/* SiKoyek V1.0 — approved lower dashboard panel layout */' not in s:
              s = s.replace('</style>', css + '</style>', 1)

          if s == original:
              print('No changes required.')
          else:
              p.write_text(s, encoding='utf-8')
              print('Dashboard lower panel patch applied.')

      - name: Commit patched dashboard
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "Nothing to commit."
          else
            git commit -m "fix dashboard lower panel layout"
            git push
          fi
