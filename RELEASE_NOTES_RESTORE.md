# UI Restore

The main `index.html` UI is restored to the pre-refactor SiKoyek V1.0 release that matched the initial accepted Dashboard/Project Detail experience.

Next fixes will be limited to percentage display and button routing; backend data remains unchanged.
